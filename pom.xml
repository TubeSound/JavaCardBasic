<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.github.tubesound</groupId>
    <artifactId>javacard-basic</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>JavaCardBasic</name>
    <description>Java Card learning exercises with a desktop simulator</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Desktop .class files for jCardSim; this does not build a card CAP file. -->
        <maven.compiler.release>17</maven.compiler.release>
        <jcardsim.version>3.0.6.0</jcardsim.version>
        <junit.version>5.10.3</junit.version>
    </properties>

    <dependencies>
        <!-- Maven Central distribution maintained at github.com/ph4r05/jcardsim. -->
        <dependency>
            <groupId>com.klinec</groupId>
            <artifactId>jcardsim</artifactId>
            <version>${jcardsim.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <mainClass>io.github.tubesound.javacardbasic.host.HelloDemo</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
